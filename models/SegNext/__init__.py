"""
SegNext implementation (MSCAN backbone + HamDecoder).

This file exists so `models/SegNext/` is a real Python package and can be
imported without modifying `sys.path` (important for Kaggle/Jupyter).
"""

