# Kaggle P100 (Pascal, sm_60) needs a CUDA build that still includes sm_60 kernels.
# Use the official CUDA 11.8 wheel index (the newer CUDA 12.x wheels often drop Pascal).
--index-url https://download.pytorch.org/whl/cu118

# Let pip resolve the latest torch/vision/audio compatible with your Python on Kaggle,
# while staying on CUDA 11.8.
torch
torchvision
torchaudio

# Transformers stack (Trainer)
transformers>=4.39,<5.0
accelerate>=0.27,<1.0
numpy==1.26.4
pillow==10.2.0
pyyaml==6.0.1
tqdm==4.66.2
matplotlib==3.8.3
